import { StyleSheet } from "react-native";

export default StyleSheet.create({
  flex: {
    display:"flex",
    justifyContent: "center",
    alignContent: "center"
  },
  moonFonts:{
      fontFamily:"moon"
  }
});
